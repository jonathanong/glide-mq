name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  security:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm

      - run: npm ci --omit=dev

      - name: Runtime dependency audit
        run: npm audit --omit=dev --audit-level=moderate

  quality:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      - name: Typecheck
        run: npm run typecheck

      - name: Lint
        run: npm run lint

      - name: Format check
        run: npx prettier --check "src/**/*.ts" "tests/**/*.ts"

  test-unit:
    needs: quality
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm

      - run: npm ci
      - run: npm run build

      - name: Run unit tests (no Valkey needed)
        run: >-
          npx vitest run
          tests/api-completeness.test.ts
          tests/az-affinity.test.ts
          tests/connection.test.ts
          tests/job.test.ts
          tests/queue.test.ts
          tests/recovery.test.ts
          tests/shutdown.test.ts
          tests/telemetry.test.ts
          tests/testing-mode.test.ts
          tests/worker-saturation.test.ts
          tests/worker.test.ts
          tests/library-source.test.ts
          tests/lua-instrument.test.ts

  test-integration:
    needs: quality
    runs-on: ubuntu-latest
    timeout-minutes: 40
    permissions:
      contents: read
      id-token: write
    strategy:
      matrix:
        node-version: [20, 22]
      fail-fast: false
    services:
      valkey:
        image: valkey/valkey:9.1.0
        options: >-
          --health-cmd "valkey-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 6379:6379
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v6
        with:
          node-version: ${{ matrix.node-version }}
          cache: npm

      - run: npm ci
      - run: npm run build

      - name: Verify Valkey standalone
        run: |
          node -e "
          const { GlideClient } = require('@glidemq/speedkey');
          (async () => {
            const c = await GlideClient.createClient({ addresses: [{ host: 'localhost', port: 6379 }] });
            console.log('PING:', await c.ping());
            c.close();
          })().catch(e => { console.error(e.message); process.exit(1); });
          "

      - name: Install Valkey binaries for cluster
        run: |
          id=$(docker create valkey/valkey:9.1.0)
          sudo docker cp "$id:/usr/local/bin/valkey-server" /usr/local/bin/valkey-server
          sudo docker cp "$id:/usr/local/bin/valkey-cli" /usr/local/bin/valkey-cli
          docker rm "$id"
          sudo chmod +x /usr/local/bin/valkey-server /usr/local/bin/valkey-cli
          valkey-server --version

      - name: Start Valkey cluster (7000-7005)
        run: bash tests/helpers/start-cluster.sh

      - name: Run integration tests (standalone + cluster)
        run: >-
          npx vitest run
          ${{ matrix.node-version == '22' && '--coverage' || '' }}
          --exclude 'tests/api-completeness.test.ts'
          --exclude 'tests/az-affinity.test.ts'
          --exclude 'tests/connection.test.ts'
          --exclude 'tests/job.test.ts'
          --exclude 'tests/queue.test.ts'
          --exclude 'tests/recovery.test.ts'
          --exclude 'tests/shutdown.test.ts'
          --exclude 'tests/telemetry.test.ts'
          --exclude 'tests/testing-mode.test.ts'
          --exclude 'tests/worker-saturation.test.ts'
          --exclude 'tests/worker.test.ts'
          --exclude 'tests/search-index.test.ts'
          ${{ matrix.node-version == '22' && '--exclude "tests/fuzzer/**"' || '' }}

      - name: Upload coverage
        if: ${{ !cancelled() && matrix.node-version == '22' }}
        uses: codecov/codecov-action@0fb7174895f61a3b6b78fc075e0cd60383518dac # v5.5.5
        with:
          files: coverage/lcov.info
          flags: integration
          fail_ci_if_error: false
          use_oidc: true

  test-lua-coverage:
    needs: quality
    runs-on: ubuntu-latest
    timeout-minutes: 40
    permissions:
      contents: read
      id-token: write
    services:
      valkey:
        image: valkey/valkey:9.1.0
        options: >-
          --health-cmd "valkey-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 6379:6379
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm

      - run: npm ci --ignore-scripts
      - run: npm run build:lua-cov

      - name: Run standalone integration with Lua coverage
        run: >-
          npx vitest run
          --exclude 'tests/api-completeness.test.ts'
          --exclude 'tests/az-affinity.test.ts'
          --exclude 'tests/connection.test.ts'
          --exclude 'tests/job.test.ts'
          --exclude 'tests/queue.test.ts'
          --exclude 'tests/recovery.test.ts'
          --exclude 'tests/shutdown.test.ts'
          --exclude 'tests/telemetry.test.ts'
          --exclude 'tests/testing-mode.test.ts'
          --exclude 'tests/worker-saturation.test.ts'
          --exclude 'tests/worker.test.ts'
          --exclude 'tests/search-index.test.ts'
          --exclude 'tests/fuzzer/**'
          --exclude 'tests/lua-instrument.test.ts'
          --exclude 'tests/library-source.test.ts'
          --exclude 'tests/producer-unit.test.ts'
        env:
          SKIP_CLUSTER: '1'
          GLIDEMQ_LUA_COVERAGE: '1'

      - name: Dump Lua coverage
        if: ${{ !cancelled() }}
        run: node scripts/dump-lua-coverage.cjs

      - name: Require Lua LCOV
        run: test -s coverage/lua.info

      - name: Upload Lua coverage
        if: ${{ !cancelled() }}
        uses: codecov/codecov-action@0fb7174895f61a3b6b78fc075e0cd60383518dac # v5.5.5
        with:
          files: coverage/lua.info
          flags: lua
          fail_ci_if_error: false
          use_oidc: true

  test-search:
    needs: quality
    runs-on: ubuntu-latest
    timeout-minutes: 15
    services:
      valkey:
        image: valkey/valkey-bundle:9.1.0
        options: >-
          --health-cmd "valkey-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 6379:6379
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run build
      - name: Run search integration tests
        run: npx vitest run tests/search-index.test.ts
        env:
          SKIP_CLUSTER: '1'
